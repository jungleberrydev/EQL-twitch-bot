services:
  twitch:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DATA_DIR: /app/data
    volumes:
      # Survives container rebuilds; rsync --exclude data keeps host ./data
      - ./data:/app/data
